# Ein Algorythmus der eine Liste, bestehend aus Integern,
# in eine neue Liste sortiert und diese am Ende ausgibt.

# Die unsortierte Liste soll in eine Funktion übergeben werden.
# Dann soll aus dieser Liste der kleinste Int in einer Variablen abgespeichert werden.
# Danach wird der Wert der Variablen an die neue Liste gehängt. ( sortierte_liste.append(Variable) )
# Ist das passiert, soll der Wert der Variablen aus der unsortierten Liste gelöscht werden. ( liste.remove(Variable) )
# Das ganze wird so lange wiederholt bis die unsortierte Liste leer ist.

# Die neue Liste sollte nun mit den gleichen Werten der alten Liste
# in aufsteigender Reihenfolge befüllt worden sein
